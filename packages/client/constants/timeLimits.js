// Fixed set of response-time options for a question.
// Kept separate from Colors/Spacing tokens since this is domain data, not design data.
export const TIME_LIMIT_OPTIONS = [60, 45, 30, 15];
export const DEFAULT_TIME_LIMIT = 45;
