branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo ""
  echo "🛑  PUSH BLOCKED"
  echo "   אסור לדחוף ישירות ל-$branch."
  echo "   פתחי ענף נפרד וצרי Pull Request."
  echo ""
  exit 1
fi
